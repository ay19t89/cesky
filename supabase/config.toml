project_id = "vmcegdasdeucdrxngjfv"

[functions.dictionary]
verify_jwt = false
# Word lookup is public. The czech_words table is accessed separately through
# authenticated Supabase clients and remains protected by per-user RLS.
